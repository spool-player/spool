<?xml version="1.0"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.sachk.spool"
    android:installLocation="auto"
    android:versionCode="-- %%INSERT_VERSION_CODE%% --"
    android:versionName="-- %%INSERT_VERSION_NAME%% --">
    <!-- %%INSERT_PERMISSIONS -->
    <!-- %%INSERT_FEATURES -->
    <!-- One package serves televisions and handsets, so neither form factor
         may be a requirement: a required leanback feature hides the app from
         phones, and a required touchscreen hides it from televisions. The app
         asks the system which one it is at runtime instead. -->
    <uses-feature android:name="android.software.leanback" android:required="false" />
    <uses-feature android:name="android.hardware.touchscreen" android:required="false" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.REQUEST_INSTALL_PACKAGES" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_MEDIA_PLAYBACK" />
    <uses-permission android:name="android.permission.WAKE_LOCK" />
    <application
        android:name="org.qtproject.qt.android.bindings.QtApplication"
        android:allowBackup="false"
        android:banner="@drawable/tv_banner"
        android:enableOnBackInvokedCallback="false"
        android:hardwareAccelerated="true"
        android:icon="@mipmap/ic_launcher"
        android:label="-- %%INSERT_APP_NAME%% --"
        android:theme="@style/AppTheme">
        <activity
            android:name="com.sachk.spool.SpoolActivity"
            android:configChanges="orientation|uiMode|screenLayout|screenSize|smallestScreenSize|layoutDirection|locale|fontScale|keyboard|keyboardHidden|navigation|mcc|mnc|density"
            android:exported="true"
            android:label="-- %%INSERT_APP_NAME%% --"
            android:launchMode="singleTop"
            android:screenOrientation="unspecified">
            <!-- Both entry points, because both launchers have to find the one
                 activity. A television resolves an unspecified orientation to
                 landscape on its own, so nothing here needs to name it. -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
                <category android:name="android.intent.category.LEANBACK_LAUNCHER" />
            </intent-filter>
            <meta-data android:name="android.app.lib_name" android:value="-- %%INSERT_APP_LIB_NAME%% --" />
            <meta-data android:name="android.app.arguments" android:value="-- %%INSERT_APP_ARGUMENTS%% --" />
            <!-- Keep Qt's command, timer and network queues alive for music and
                 system media controls while the Activity is suspended. -->
            <meta-data android:name="android.app.background_running" android:value="true" />
            <meta-data android:name="android.app.splash_screen_drawable" android:resource="@drawable/spool_splash" />
            <meta-data android:name="android.app.splash_screen_sticky" android:value="true" />
        </activity>
        <service
            android:name="com.sachk.spool.LocalMediaPlaybackService"
            android:exported="false"
            android:foregroundServiceType="mediaPlayback" />
        <receiver
            android:name="com.sachk.spool.LocalMediaPlaybackService$ControlReceiver"
            android:exported="false" />
        <receiver
            android:name="com.sachk.spool.RemoteMediaSessionBridge$ControlReceiver"
            android:exported="false" />
        <provider
            android:name="androidx.core.content.FileProvider"
            android:authorities="${applicationId}.qtprovider"
            android:exported="false"
            android:grantUriPermissions="true">
            <meta-data android:name="android.support.FILE_PROVIDER_PATHS" android:resource="@xml/update_provider_paths" />
        </provider>
    </application>
</manifest>
